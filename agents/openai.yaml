interface:
  display_name: "Agent Query Layer Skill Pack"
  short_description: "Use gateway REST, WebSocket, and JSON-RPC for market-read workflows."
  default_prompt: "Use $agent-query-layer-skill-pack when you need Agent-Query-Layer token price, OHLCV, token profile, liquidity, pool metadata, symbol-first CEX asset or market queries, realtime subscriptions, or hosted chain compatibility through the product gateway surfaces."
